---
name: prompt-optimizer
description: 优化用户的提示词。当用户要求优化提示词、改进 prompt、写提示词、或者输入简单提示词时触发。基于模型能力对提示词进行优化，如果用户描述不清楚会分步澄清。
---

# 提示词优化器

基于模型能力对用户输入的简单提示词进行优化，生成更清晰、更具体、更有效的提示词。如果用户描述不够清楚，会主动分步与用户澄清。

## 触发场景

当用户有以下意图时，应触发此技能：
- 明确要求优化提示词：「优化这个 prompt」、「帮我改进提示词」、「优化一下这句话」
- 要求写提示词：「帮我写个提示词」、「给我一个 prompt」
- 输入简单提示词后期望改进
- 表达对提示词效果不满意：「效果不好」、「生成的内容不够好」

## Instructions

当检测到用户需要优化提示词时，按以下步骤执行：

### 1. 分析用户提示词

首先分析用户输入的提示词，检查是否存在以下问题：
- **目标不明确**：没有说明期望的输出或结果
- **缺少上下文**：缺少必要的背景信息
- **缺少约束**：没有格式、长度、风格等约束
- **角色缺失**：没有指定 AI 应该扮演的角色
- **示例缺失**：复杂任务缺少示例说明

### 2. 判断是否需要澄清

**如果用户提示词足够清晰**，直接进入优化步骤。

**如果用户提示词模糊或不完整**，需要分步澄清。根据对提示词的理解，提出 **1-3 个** 最关键的问题：

澄清问题示例：
- 「请问你的目标受众是谁？（如：初学者、专业人士、学生）」
- 「你期望的输出格式是什么？（如：代码、文章、列表、表格）」
- 「有哪些特定的约束条件？（如：字数限制、风格要求、必须包含的要点）」
- 「这个提示词用于什么场景？（如：代码生成、文案创作、数据分析、学习辅导）」

**注意**：不要一次性问太多问题，聚焦在最关键的 1-3 个问题上。

### 3. 优化提示词

基于用户反馈和原始提示词，使用以下框架进行优化：

#### 提示词优化框架

```markdown
# Role (角色)
你是一位 [具体角色]，擅长 [相关技能]

# Task (任务)
[清晰描述任务目标]

# Context (背景)
[必要的上下文信息]

# Constraints (约束)
- [约束条件 1]
- [约束条件 2]
- ...

# Output Format (输出格式)
[期望的输出格式和结构]

# Examples (示例，可选)
输入示例：...
期望输出：...
```

### 4. 提供优化版本

提供 **1-2 个** 优化版本：
- **标准版**：通用、平衡的优化版本
- **进阶版**（可选）：包含更详细约束和示例的版本

每个版本都应该：
- 保留用户原始意图
- 添加必要的上下文和约束
- 使用清晰的结构化格式
- 便于直接使用

### 5. 解释优化点

简要说明做了哪些优化以及为什么：
- 添加了什么元素
- 为什么这样改
- 预期效果是什么

## 示例

### 示例 1：简单提示词优化

**用户输入**：「优化这个：写一个 python 函数」

**Agent 回应**：
```
我来帮你优化这个提示词。目前这个提示词比较简略，我需要了解更多信息：

1. 这个函数要实现什么功能？（如：数据处理、文件操作、API 调用等）
2. 输入和输出是什么？
3. 有没有特定的要求或约束？（如：性能要求、错误处理、代码风格）

请告诉我这些信息，我会帮你生成一个更完整的提示词。
```

### 示例 2：中等提示词优化

**用户输入**：「优化：帮我写一个产品描述」

**Agent 分析后直接优化**：

```
## 优化后的提示词（标准版）

```
你是一位专业的电商文案撰写人，擅长用简洁有力的语言突出产品卖点。

请为以下产品撰写产品描述：
- 产品名称：[填写]
- 目标受众：[填写]
- 核心功能/卖点：[填写]

要求：
1. 突出产品的核心优势和差异化特点
2. 使用生动、具体的语言，避免空洞的形容词
3. 包含使用场景描述，让用户产生代入感
4. 字数控制在 150-200 字

输出格式：
- 标题（15 字以内，吸引眼球）
- 正文（3-4 个卖点，每个卖点 1-2 句话）
- 行动号召（引导购买）
```
```

### 示例 3：代码生成提示词优化

**用户输入**：「写个快排」

**Agent 优化**：

```
我来帮你优化这个提示词，让它能生成更高质量的代码：

## 优化后的提示词

```markdown
你是一位资深 Python 工程师，擅长算法实现和代码教学。

请用 Python 实现快速排序算法，要求：

1. 实现完整的快速排序函数，包含必要的参数和返回值类型注解
2. 添加详细的注释，解释关键步骤和算法原理
3. 处理边界情况（空列表、单元素、重复元素等）
4. 提供时间复杂度和空间复杂度分析
5. 包含 2-3 个使用示例

输出格式：
- 算法原理简介（1-2 句话）
- 完整代码实现（带类型注解和注释）
- 复杂度分析
- 使用示例
```
```

## 最佳实践

- **保持对话式**：用友好的语气与用户交流，不要生硬
- **循序渐进**：复杂提示词可以分多轮迭代优化
- **解释原因**：告诉用户为什么要这样改，帮助用户学习
- **保留原意**：优化不是重写，要忠实于用户的原始意图
- **灵活应变**：根据用户的反馈调整优化策略

## 注意事项

- 如果用户明确表示就是想要简单的提示词，不要过度优化
- 对于技术类提示词，确保术语准确
- 对于创意类提示词，保留足够的发挥空间
- 始终提供可以直接使用的完整提示词